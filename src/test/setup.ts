// Convención de testing pinneada en Fase 3 (spine, Deferred): Vitest +
// Testing Library + jsdom. Este archivo carga los matchers de jest-dom
// (toBeInTheDocument, etc.) para todos los tests.
import "@testing-library/jest-dom/vitest";
